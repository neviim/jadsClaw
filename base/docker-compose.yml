services:
  openclawd:
    image: iapalandi/openclaw:latest
    container_name: openclaw_core
    networks:
      - openclaw_internal
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "1.5"

networks:
  openclaw_internal:
    driver: bridge
