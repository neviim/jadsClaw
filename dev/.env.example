# =============================================================================
# OpenClaw — Variaveis de Ambiente (DESENVOLVIMENTO)
# Copie este arquivo para .env e substitua os valores placeholder
# Comando: cp .env.example .env
# =============================================================================

# --- OPENCLAW GATEWAY ---
# Token de acesso ao gateway (obrigatorio para acesso remoto)
# Para gerar um token:  openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=seu_token_gateway_aqui

# Senha do gateway — necessaria para acessar a Control UI no navegador.
# Sem esta senha, o navegador mostra: "unauthorized: gateway password missing"
# Para gerar uma senha legivel:
#   cat /dev/urandom | tr -dc 'A-Z0-9' | fold -w 4 | head -n 4 | paste -sd '-'
#   Exemplo de resultado: K7X2-9BM4-QP3L-W8TF
OPENCLAW_GATEWAY_PASSWORD=sua_senha_gateway_aqui

# Porta do gateway no host (default: 18789)
OPENCLAW_PORT=18789

# --- CORE API ACCESS ---
ANTHROPIC_API_KEY=sua_chave_claude
GEMINI_API_KEY=sua_chave_gemini
ZAI_API_KEY=sua_chave_z_ai

# --- CHANNELS ---
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=seu_token_telegram
TELEGRAM_ALLOWED_USERS=seu_id_usuario

DISCORD_ENABLED=false
DISCORD_BOT_TOKEN=seu_token_discord
DISCORD_GUILD_ID=seu_guild_id_discord

NOSTR_ENABLED=false
# Chave privada Nostr (nsec) — gerar com: docker exec openclaw_core node -e "..."
# Ver doc/13-configura_nostr.md para instrucoes completas
NOSTR_PRIVATE_KEY=sua_chave_privada_nostr_nsec
# Chave publica (npub) do utilizador autorizado a enviar DMs ao bot
NOSTR_ALLOWED_NPUB=sua_npub_pessoal

# --- WEB SKILLS ---
SEARCH_ENGINE_API=google_ou_tavily
SEARCH_API_KEY=sua_chave_busca

# --- DEV CONFIG ---
DEBUG=true
LOG_LEVEL=debug
